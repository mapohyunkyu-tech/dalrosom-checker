# 달로썸 검수기 v10.0.30

- v10.0.26 기반
- OpenAI API 직접 연결 추가
- ③ 초안 프롬프트 생성 후 API로 바로 초안 생성 가능
- 홈피드형 초안도 API로 바로 생성 가능
- ⑧ Claude 보강·자연화 패키지를 OpenAI API로 바로 실행 가능
- API 키는 코드/JSON/ZIP에 저장하지 않고 화면 입력값, Streamlit secrets, OPENAI_API_KEY 환경변수만 사용
- requirements.txt에 openai 추가


## v10.0.28 DB 라우터 선별 엔진
- 전체 감정·귀찮음·행동 DB와 사용자 이상문장 DB를 API에 한 번에 던지지 않고, 분야/사용처/글성격/글결/작성자 관점에 맞는 조각만 선별해 프롬프트에 삽입합니다.
- 병원·법률·금융·투자 등 민감 업종은 직접 체험처럼 보이는 몸 반응/후폭풍 표현을 제외하고, 비교 피로·확인 강박·돈 고민·상담 전 망설임 위주로 낮춰 사용합니다.
- 사용자 이상문장 DB는 현재 분야와 가까운 금지 패턴만 8~10개 수준으로 선별해 생성 단계에서 피하도록 합니다.
- 기존 복붙 프롬프트와 API 생성 모두 같은 DB 라우터 블록을 사용합니다.


## v10.0.30 국어선생님 2차 재검수 반복 방지 핫픽스
- ⑩ 국어선생님 최종 퇴고본에서 문장 중간에 강제로 줄바꿈되던 문제를 수정했습니다.
- 분석용 문장 분리 로직은 검수 리포트/수정 제안에만 사용하고, 최종 원고 출력에는 적용하지 않습니다.
- 문단 사이에는 빈 줄 1개만 남기고, 소제목은 한 줄 단독으로 유지합니다.
- `result = "\n".join(sentences)` 계열의 문장 단위 최종 출력 방식을 제거했습니다.
- 최종 출력 전 `비교하다 / 보면`, `샷 수보다 / 피부 두께`, `설계되어 / 있어` 같은 soft line break를 복구합니다.
