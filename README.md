# 달로썸 원고 검수기 v10.0.18

## v10.0.18 오탐 수정 패치
- `충분한가요/충분하다`가 `분하다` 의료 감정선 오류로 오탐되는 문제 수정
- `무조건 좋다는 오해`, `무조건 더 좋다고 보기 어렵다`처럼 반박 문맥의 `무조건`을 위험표현에서 제외
- 실제 결과 보장 문맥의 `무조건 효과/무조건 개선/무조건 승소` 등은 계속 위험 처리
- 키워드 배치 검수에서 소제목 바로 아래 본문이 붙어 있어도 섹션을 줄 단위로 다시 인식하도록 보정
- ⑧ 검수 결과 다운로드 기능은 유지

## 테스트
- `python -m py_compile app.py` 통과
- `run_menu_fake_test.py` 통과
- `run_integration_harmony_check.py` 통과
- `run_v1018_target_tests.py` 통과
